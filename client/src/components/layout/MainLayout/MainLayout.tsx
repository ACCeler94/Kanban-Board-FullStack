import Navbar from '../Navbar/Navbar';
import SideBar from '../SideBar/SideBar';

const MainLayout = () => {
  return (
    <div>
      <Navbar />
      <SideBar />
    </div>
  );
};

export default MainLayout;
